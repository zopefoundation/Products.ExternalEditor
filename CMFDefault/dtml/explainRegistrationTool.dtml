<dtml-var manage_page_header>
<dtml-var manage_tabs>

<h3> <code>portal_registration</code> Tool </h3>

<p> This tool embodies the policies of the CMFSite with respect to
    joining / adding a new member;  in particular, it sets the default
    policies for allowable member IDs and passwords.
</p>

<dtml-var manage_page_footer>

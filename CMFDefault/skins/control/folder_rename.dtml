<dtml-call expr="manage_renameObject( REQUEST['id'], REQUEST['new_id'] )">
<dtml-call expr="REQUEST['RESPONSE'].redirect( absolute_url()
                     + '/folder_contents'
                     + '?portal_status_message=Renamed.' )">
